# Private Beta Readiness — Baseline Diagnostic

> Generated by Kilo/Kimi private-beta readiness pass on branch `beta/private-readiness-kilo-kimi`.
> Do not edit manually without updating evidence links.

## Start SHA

- `a220cbefcc3247804138f146ef01953d0a25ef60` (`main` HEAD before changes)
- Branch created: `beta/private-readiness-kilo-kimi`

## Environment

- Host platform: Windows (win32)
- Python available: `3.11.15` (repo recommends **3.12**)
- `pip 26.1.2` available
- `ruff 0.15.8` available
- `pytest 9.0.3` already installed locally (repo pins `pytest==7.4.4`)
- `ripgrep 15.1.0` available
- `gh CLI 2.89.0` available
- `git 2.53.0.windows.2` available

## Commands attempted / status

| Command | Status | Notes |
|---------|--------|-------|
| `python --version` | OK | `3.11.15` (not 3.12) |
| `python -m pip install -r requirements.txt` | IN-PROGRESS | Background install running; many packages already present locally |
| `python scripts/validate_local.py --quick` | MISSING | Script does not exist |
| `ruff check .` | NOT YET RUN | Will run after dependency install |
| `pytest -q` | NOT YET RUN | Will run after dependency install |
| `python scripts/seed_bea_memory.py --report --profile public` | MISSING | Script does not exist |
| `python scripts/audit_memory_store.py --dry-run --privacy-scan --json` | MISSING | Script does not exist |
| `python scripts/check_client_v1_usage.py` | MISSING | Script does not exist |
| `python scripts/check_policy_principal_binding.py` | MISSING | Script does not exist |
| `python scripts/check_tool_executor_mission_id.py` | MISSING | Script does not exist |
| `rg "sk-\\|OPENROUTER_API_KEY=\\|Bearer \\|api_key\\|secret\\|token" .` | BLOCKED | Syntax issues in PowerShell; retry with Python wrapper |
| `rg "production ready\\|stable public beta\\|guaranteed\\|fully autonomous" README.md ...` | OK | Found `guaranteed` in archived doc (non-blocking with note) |

Evidence for missing commands:

- `glob('scripts/validate_local.py')` → no match
- `glob('scripts/seed_bea_memory.py')` → no match
- `glob('scripts/audit_memory_store.py')` → no match
- `glob('scripts/check_*.py')` → no match

## Existing documentation (requested context files)

| File | Exists? | Status |
|------|---------|--------|
| `README.md` | Yes | Generic monorepo overview; mentions "CI local reproductible (P0)" |
| `README_PUBLIC_BETA.md` | **No** | Must be created (LOOP 4) |
| `PUBLIC_BETA_CHECKLIST.md` | **No** | Must be created (LOOP 1) |
| `docs/STATUS.md` | Yes | Claims 🟢 PROVEN for core; mentions production-ready language (to reconcile) |
| `docs/ALPHA_READINESS.md` | **No** | N/A; replaced by private-beta checklist |
| `docs/API_VERSIONING.md` | Yes | v1 deprecated, v2 stable, v3 preferred; action items not all done |
| `docs/KNOWN_LIMITATIONS.md` | **No** | Must be created (LOOP 4) |
| `docs/PRIVACY_FOR_TESTERS.md` | **No** | Must be created (LOOP 4) |
| `docs/BETA_TESTER_GUIDE.md` | **No** | Must be created (LOOP 4) |
| `docs/FEEDBACK_GUIDE.md` | **No** | Must be created (LOOP 4) |
| `docs/TROUBLESHOOTING.md` | **No** | Must be created (LOOP 4) |
| `docs/PRIVATE_BETA_RUNBOOK.md` | **No** | Must be created (LOOP 4) |
| `SECURITY_MODEL.md` | **No** | Must create or reference `docs/SECURITY_AUDIT.md` |
| `.env.example` | Yes | Placeholders only; `JARVIS_PRODUCTION=true` line 67 is risky for copy-paste beta users |
| `.env.production.example` | Yes | Minimal; does not reference auth tokens (must be improved) |
| `.gitignore` | Yes | `.env`, `.env.*.local`, `.env.production`, `.env.backup*`, `.env.test`, `.env.agents` all ignored |
| `.gitleaks.toml` | Yes | Extends default, allowlists `.secrets.baseline`, `docs/archive/`, `tests/`, `.env.example` |
| `.github/workflows/ci.yml` | Yes | gitleaks + ruff + pytest + coverage gate (55%) + Docker build test |
| `.github/workflows/pr-smoke.yml` | **No** | Does not exist (only `ci.yml`, `unit.yml`, `kernel_ci.yml`, `pre-commit.yml`, `deploy.yml`, `flutter_apk.yml`) |
| `.github/ISSUE_TEMPLATE/*` | **No** | Must be created (LOOP 5) |

## Existing validation/test scripts

- `scripts/ci/local_ci.sh` — bash CI replica (lint + pytest + cov gate)
- `scripts/ci/local_tests_only.sh` — tests-only bash helper
- `scripts/verify_boot.sh` — boot sanity
- `scripts/verify_prod.sh` — prod checks
- `scripts/verify-e2e.sh` — e2e checks
- `scripts/safety_check.py` — runtime safety script
- `scripts/activate_si_v3.sh` — **actively enables self-improvement by editing `.env`** (must be gated/documented)
- `scripts/run_proactive_agent.py`, `scripts/memory_manual_test.py`, etc. — manual/debug scripts

**Missing for beta readiness**: memory seed/audit, policy binding checks, client v1 usage check, tool executor mission-id check, unified private-beta gate.

## Security / self-improvement defaults

- `config/settings.py` line 222: `self_improve_enabled: bool = field(default_factory=lambda: _b("SELF_IMPROVE_ENABLED", "true"))`
  - **BLOCKER P0**: self-improvement is enabled by default.
- `scripts/activate_si_v3.sh` line 57: forcibly sets `SELF_IMPROVE_ENABLED=true` and `DRY_RUN=false`.
  - **BLOCKER P0/P1**: dangerous for private beta if run without warnings.
- `main.py` (entrypoint) overrides `SELF_IMPROVE_ENABLED=false` when `JARVIS_PRODUCTION=true` (lines 344-353).
  - This is a mitigation, but default non-production runs still enable SI.
- `BEA_CONTINUOUS_IMPROVEMENT` and `BEA_SKIP_IMPROVEMENT_GATE` env vars:
  - Only mentioned in untracked `scripts/seed_bea_knowledge_claude.py` (line 513).
  - Not implemented in `config/settings.py`; must be introduced as canonical beta-safe aliases.

## Auth / public-path audit

Verified with `python -c "from api.access_enforcement import is_public_path; ..."`:

| Path | Public? | Gap |
|------|---------|-----|
| `/health` | Yes | OK |
| `/api/health` | Yes | OK (legacy dev alias) |
| `/api/v2/health` | Yes | OK |
| `/api/v3/system/readiness` | Yes | Docker healthcheck / verify_boot usage |
| `/` | Yes | SPA root |
| `/app.html` | Yes | SPA login |
| `/auth/login` | Yes | OAuth2 login |
| `/auth/token` | Yes | OAuth2 token |
| `/docs`, `/redoc`, `/openapi.json` | Yes | Only when `ENABLE_API_DOCS=1` (prod disables) |
| `/metrics` | **No** | OK — protected by middleware |
| Static assets (`/static/*`, `*.css`, etc.) | Yes | OK |

Auth enforcement uses `AccessEnforcementMiddleware` + `require_auth` dependency. Failure mode: many routers are imported in `try/except`, silently disabling whole surfaces if import fails. Not a direct auth bypass, but operational risk.

## Claims audit

- `docs/STATUS.md` line 248: "The Jarvis cognitive core is **PROVEN and stable**" and earlier uses "Production-grade".
  - Reconcile with honest private-beta claims in docs/checklist.
- `docs/archive/PHASE4_FINAL_REPORT.txt` line 116: "100% availability guaranteed" (archived).
  - Acceptable as archive, but gate script should skip `docs/archive/`.
- `CHANGELOG.md` line 71: "drift was guaranteed" — benign usage of word "guaranteed".

## Open local issues / PRs

- `git status --short` on start branch showed:
  - Modified: `core/self_improvement/weakness_detector.py` (3-line fix, legitimate)
  - Untracked: `scripts/ingest_mistral_toolkit.py`, `scripts/seed_bea_knowledge_claude.py`
- No local open PRs can be detected from repo alone.
- GitHub CLI will be used in LOOP 6 to triage remote issues/PRs.

## P0/P1 blockers identified

### P0 — Must fix before private beta

1. **Self-improvement enabled by default** (`SELF_IMPROVE_ENABLED=true` default).
2. **No unified private-beta gate script** (`scripts/private_beta_gate.py` missing).
3. **No public-beta/private-beta checklist** (`PUBLIC_BETA_CHECKLIST.md` missing).
4. **No honest beta-tester docs** (`README_PUBLIC_BETA.md`, `BETA_TESTER_GUIDE.md`, etc. missing).
5. **No issue templates** for safe bug/security/feedback reports.
6. **Env example ships `JARVIS_PRODUCTION=true`** as default (line 67) — dangerous for copy/paste.
7. **Untracked scripts** (`seed_bea_knowledge_claude.py`) mention `BEA_CONTINUOUS_IMPROVEMENT` but are not integrated; risk of confusion.
8. **No memory hygiene scripts/tests** for public-safe seeding.

### P1 — Should fix before inviting testers

1. `scripts/activate_si_v3.sh` can silently enable `DRY_RUN=false + SELF_IMPROVE_ENABLED=true`; needs warning/gate for beta.
2. `docs/STATUS.md` overclaims maturity; reconcile with beta language.
3. Docker build test in `ci.yml` uses `sqlite:///test.db` and does not run full health probe.
4. Many API routers imported with `try/except` fail-open silently.
5. `api/routes/extensions.py`, `metrics_mobile.py`, `venture.py` flagged in `docs/STATUS.md` as HIGH security issues (no/partial auth).
6. Python 3.11 local vs 3.12 CI drift — document support matrix.

## Human / external actions required

- Rotate any active secrets that may have been exposed historically (`JWT_SECRET_KEY`, `JARVIS_API_TOKEN`, `OPENROUTER_API_KEY`, `POSTGRES_PASSWORD` per `docs/SECURITY_AUDIT.md` §7-8).
- Revoke hardcoded Flutter token `jv-uAu...CXPkcfJLg` if still active (per `docs/SECURITY_AUDIT.md` §8.7).
- Confirm force-push of cleaned git history to `origin/main` if not already done.
- Provide 5–10 testers and a private communication channel.

## Baseline conclusion

- **Private beta ready: NO**
- **Public beta ready: NO**
- The repo has a solid core but lacks the honest gates, documentation, and fail-closed defaults required for a safe private beta. The gaps are fixable in this branch by implementing the 7 loops described in the mission.
