# Core dependencies
python-dotenv==1.0.0
pyyaml==6.0.1
requests>=2.32.5

# Database
psycopg2-binary==2.9.9
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.13.1

# Cache & Queue
redis==5.0.1
celery==5.3.4

# Web framework
# fastapi 0.109.0 → 0.109.1 (PYSEC-2024-38)
# TODO(deferred): bump fastapi 0.109 → 0.115+ pour pull starlette 0.40+
# (CVE-2024-47874, CVE-2025-54121) — breaking: Pydantic v2 lifecycle,
# route signatures, OAuth2 flows. À planifier en PR dédiée.
fastapi==0.109.1
uvicorn[standard]==0.42.0
websockets==12.0
# python-multipart 0.0.22 → 0.0.26 (CVE-2026-40347)
python-multipart==0.0.26

# Data processing
beautifulsoup4==4.12.3
lxml==5.1.0
pandas==2.2.0
playwright==1.41.2

# Security
# cryptography 42.0.2 → 43.0.1 (GHSA-h4gh-qq45-vh27 + PYSEC-2024-225).
# TODO(deferred): bump to 46.0.6 pour fermer CVE-2024-12797, CVE-2026-26007,
# CVE-2026-34073 — nécessite validation des breaking changes côté asyncpg/
# jwt/TLS. À faire dans une PR dédiée avec tests d'intégration.
cryptography==43.0.1
# pyjwt 2.8.0 → 2.12.0 (CVE-2026-32597)
pyjwt==2.12.0

# Payments
stripe==8.0.0

# Monitoring
prometheus-client==0.19.0

# Testing
# TODO(deferred): pytest 7.4.4 → 9.0.3 (CVE-2025-71176). Bump major, risque
# de breaking sur conftest/plugins ; à planifier avec un run complet ciblé.
pytest==7.4.4
pytest-asyncio==0.23.3

# Utilities
psutil==5.9.8
# jinja2 3.1.3 → 3.1.6 (CVE-2024-34064, CVE-2024-56201, CVE-2024-56326, CVE-2025-27516)
jinja2==3.1.6

# Graph / causal reasoning
networkx>=3.2
numpy>=1.24.0
openai>=1.0.0

# LLM & AI — required at runtime (versions aligned with requirements.lock)
structlog==25.5.0
langchain==1.2.15
# langchain-core 1.2.25 → 1.2.31 (CVE-2026-40087, min requis par langchain-openai 1.1.14)
langchain-core==1.2.31
# langchain-openai 1.1.12 → 1.1.14 (GHSA-r7w7-9xr2-qq2r)
langchain-openai==1.1.14
langchain-community==0.4.1
langchain-ollama==1.0.1
langgraph==1.1.5
ollama==0.6.1
qdrant-client==1.17.1
httpx==0.27.2
aiohttp==3.13.5
slowapi==0.1.9
mcp==1.27.0
sentence-transformers>=2.7.0
docker>=7.0.0
