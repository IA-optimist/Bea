# Core dependencies
# python-dotenv 1.0.0 → 1.2.2 (CVE-2026-28684, quick close: pure-Python lib)
python-dotenv==1.2.2
pyyaml==6.0.1
requests==2.34.2

# Database
psycopg2-binary==2.9.9
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.13.1

# Cache & Queue
redis==5.0.1
celery==5.3.4

# Web framework
# C4 cycle 2 (audit follow-up): bumped 0.115.6 → 0.136.3 to close three
# more Starlette CVEs that surfaced after the C4 cycle 1 bump:
#   - PYSEC-2026-161 (fix: starlette 1.0.1)
#   - CVE-2025-54121 (fix: starlette 0.47.2) — was misreported as closed in C4 cycle 1
#   - CVE-2025-62727 (fix: starlette 0.49.1)
# Side-venv dry-run (docs/security/c4-fastapi-115-migration.md) re-ran
# under 0.136.3 + starlette 1.2.0: 13/13 module imports clean, 84/84
# hardening tests pass, no concrete breaks. The 1-line projects.py fix
# from cycle 1 (response_model=None, response_class=Response) still
# applies and continues to work.
fastapi==0.136.3
# Starlette pinned explicitly for supply-chain visibility; FastAPI 0.136.3
# pulls 1.2.x transitively.
starlette==1.2.0
uvicorn[standard]==0.42.0
websockets==12.0
# python-multipart 0.0.22 → 0.0.26 (CVE-2026-40347)
python-multipart==0.0.29

# Data processing
beautifulsoup4==4.12.3
# lxml 5.1.0 → 6.1.0 (PYSEC-2026-87). Validated 2026-05-29 in side-venv:
#   - lxml.etree + lxml.html imports clean
#   - bs4 4.12.3 with `BeautifulSoup(..., "lxml")` parser works
#   - 94/94 hardening tests pass under lxml 6.1.0
lxml==6.1.0
pandas==2.2.0
playwright==1.41.2

# Security
# cryptography 43.0.1 → 46.0.6 (PYSEC-2026-35, CVE-2024-12797, CVE-2026-26007).
# Validated 2026-05-29 in .venv-c4-prep:
#   - 94/94 hardening tests pass
#   - AES-GCM encrypt/decrypt round-trip OK (core/security/secret_crypto.py)
#   - pyjwt 2.13.0 HS256 round-trip OK
#   - asyncpg 0.31.0 imports clean (SCRAM uses cryptography ≥ 41 — no break)
#   - Smoke harness 13/13 module imports clean
cryptography==46.0.6
# pyjwt 2.8.0 → 2.12.0 (CVE-2026-32597)
pyjwt==2.12.0

# Payments
stripe==8.0.0

# Monitoring
prometheus-client==0.19.0

# Testing
# pytest 7.4.4 → 9.0.3 (CVE-2025-71176). Validated 2026-05-29 in side-venv:
#   - 94/94 hardening tests pass (conftest pre-imports + JARVIS_API_TOKEN
#     env injection unchanged)
#   - pytest-asyncio bumped to 0.25 for pytest 9 compat
pytest==9.0.3
pytest-asyncio==0.25.0

# Utilities
psutil==5.9.8
# jinja2 3.1.3 → 3.1.6 (CVE-2024-34064, CVE-2024-56201, CVE-2024-56326, CVE-2025-27516)
jinja2==3.1.6

# Graph / causal reasoning
networkx==3.6.1
numpy==1.26.4
openai==2.37.0

# LLM & AI — required at runtime (versions aligned with requirements.lock)
structlog==25.5.0
langchain==1.2.15
# langchain-core 1.2.25 → 1.2.31 (CVE-2026-40087, min requis par langchain-openai 1.1.14)
langchain-core==1.2.31
# langchain-openai 1.1.12 → 1.1.14 (GHSA-r7w7-9xr2-qq2r)
langchain-openai==1.1.14
langchain-community==0.4.1
langchain-ollama==1.0.1
langgraph==1.1.5
ollama==0.6.1
qdrant-client==1.17.1
httpx==0.27.2
aiohttp==3.13.5
slowapi==0.1.9
mcp==1.27.0
sentence-transformers==5.5.0
docker==7.1.0
