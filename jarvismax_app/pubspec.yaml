name: jarvismax_app
description: JarvisMax — Application de contrôle Android
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.5.5
  provider: ^6.1.2
  intl: ^0.19.0
  web_socket_channel: ^2.4.0
  flutter_secure_storage: ^9.0.0
  connectivity_plus: ^6.0.0
  flutter_local_notifications: ^17.2.4   # Notifications locales (approval alerts)
  flutter_markdown: ^0.7.6                # Rendu markdown dans les réponses

flutter:
  uses-material-design: true
