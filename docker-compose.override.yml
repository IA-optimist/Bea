version: '3.9'
services:
  api:
    ports:
      - '8000:8000'
    volumes:
      - .:/app
    environment:
      # Profil VPS: max_agents=8 au lieu de 2 (LOCAL) -> crews complexes fonctionnent
      - JARVIS_MODE=vps
      - RESOURCE_MAX_AGENTS=8
